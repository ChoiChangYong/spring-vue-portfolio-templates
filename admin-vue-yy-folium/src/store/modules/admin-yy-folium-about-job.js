const state = {
    jobs: [{index: 0, job: ""}]
}
const getters = {
    getJobs(state) {
        return state.jobs
    }
}

const mutations = {
    
}

export default {
    state,
    getters,
    mutations
}